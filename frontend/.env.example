# Plantilla de configuracion del frontend. Copiar a .env y completar.
# OJO: todo lo que empieza con VITE_ queda incrustado en el bundle y es
# visible desde el navegador. Nunca poner secretos aca.

# URL base de la API del backend (sin barra final)
VITE_API_URL=http://localhost:8080
